import os
from telegram import Bot

# Get your bot credentials from environment variables
TOKEN = os.getenv("TELEGRAM_TOKEN")
CHAT_ID = os.getenv("CHAT_ID")

bot = Bot(token=TOKEN)
bot.send_message(chat_id=CHAT_ID, text="✅ Bot connected successfully via Render!")
